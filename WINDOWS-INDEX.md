# Реестр окон - barbershop-alikhan-mvp

**12.08.2026:** файлы колонки "Промпт-файл" переехали из корня в `брифы/` (деклаттеринг корня проекта, без изменения содержимого) - `брифы/ПРОМПТ-ОКНО-N-...md`.

Один источник правды "что сделано и где оно живёт", вместо чтения 41 файла `ПРОМПТ-ОКНО-*.md`
или прозы `tasks/today.md`. Собрано 07.08.2026 из git-истории (`origin/main`, reflog) и живых
curl-проверок прод-эндпоинтов (Amvera + GitHub Pages), не из пересказа памяти - там, где статус
подтверждён живой проверкой, это отмечено явно.

**Как читать колонку "Прод":** GH = GitHub Pages (фронтенд), AM = Amvera (бэкенд). ✅ = подтверждено
живой проверкой в этом аудите. 📋 = только со слов лога/памяти, живьём не проверялось в этом аудите.
⚠️ = лог утверждает одно, живая проверка показала другое (лог устарел).

## ⚠️ Найдено в этом аудите: 2 записи в today.md устарели

- **Окно 39** (карточка клиента, риск ухода) - `today.md` пишет "готово локально, не задеплоено".
  Живая проверка 07.08.2026: `assets/crm-clients.js` отдаёт 200 на GitHub Pages, `GET /clients?risk=true`
  отдаёт 401 (не 404) на Amvera - роут существует и живёт в проде. **Уже на проде**, запись в
  today.md не обновили после того, как коммит `358c27b` попал в общий push.
- **Окно 41** (design tokens + app shell владельца) - `today.md` пишет "не задеплоено, коммит
  локальный". Живая проверка: `crm-app-shell.css`/`crm-app-shell.js` отдают 200 на GitHub Pages,
  маркер `crm-app-shell` есть в живом `crm-owner.html`. **Уже на проде** (коммит `12a7fdd` ушёл
  отдельным push в `origin/main` вместе с последующей декомпозицией `crm-auth.js`).

Причина расхождения одна и та же: разработка → push делается одним пакетом ("заодно" уезжает и
то, что раньше не задеплоили), а текстовая запись о статусе - нет. **Вывод для практики:** после
любого `git push origin main` сверять, не уехало ли заодно что-то из "не задеплоено" выше по
истории - это регулярный источник ложной информации о состоянии прода.

## Реестр (41 окно + 2 структурных этапа рефакторинга + Этапы A и B мультиарендности)

| № | Дата | Промпт-файл | Суть | Прод |
|---|---|---|---|---|
| 1 | 24.07 | ПРОМПТ-ОКНО-1.md | Первая версия лендинга/демо | GH ✅ (ранняя фаза, деплой каждым коммитом) |
| 2 | 24.07 | ПРОМПТ-ОКНО-2.md | Правки лендинга | GH ✅ |
| 3 | 24.07 | ПРОМПТ-ОКНО-3.md + 3-ФИКС | Правки + фикс | GH ✅ |
| 4 | 24.07 | ПРОМПТ-ОКНО-4.md | Правки лендинга | GH ✅ |
| 5 | 24.07 | ПРОМПТ-ОКНО-5.md | Правки лендинга | GH ✅ |
| 6 | 27.07 | ПРОМПТ-ОКНО-6-РЕАЛИЗАЦИЯ-AWWWARDS.md | Премиум-редизайн лендинга | GH ✅ |
| 7 | 27.07 | ПРОМПТ-ОКНО-7-BACKEND-FOUNDATION.md | Бэкенд-фундамент, Amvera+Postgres вместо localStorage | GH ✅ · AM 📋 |
| 8 | 27.07 | ПРОМПТ-ОКНО-8-РОЛИ-И-СХЕМА.md | Роли, логин-гейт CRM | GH ✅ · AM 📋 |
| 9 | 27.07 | ПРОМПТ-ОКНО-9-ВИЗУАЛЬНЫЙ-МАКЕТ-CRM.md | Визуальный макет CRM поверх боевой базы | GH ✅ · AM 📋 |
| 10 | 30.07 | ПРОМПТ-ОКНО-10-МАСТЕРА-ПРАЙС-ЗАРПЛАТА.md | Реальные мастера/прайс/ЗП (ответы Алихана 30.07) | GH ✅ · AM 📋 |
| 11 | 30.07 | ПРОМПТ-ОКНО-11-НЕСКОЛЬКО-УСЛУГ.md | Несколько услуг за визит | GH ✅ · AM 📋 |
| 12 | 30.07 | ПРОМПТ-ОКНО-12-SEO-ТЕХНИКА.md | SEO-техника | GH ✅ · AM 📋 |
| 13 | 01.08 | ПРОМПТ-ОКНО-13-КОРРЕКТИРОВКА-01-08.md | Корректировка по ответам Алихана (1 точка, отмена 2ч, портфолио) | GH ✅ · AM 📋 |
| 14 | 02.08 | ПРОМПТ-ОКНО-14-ПРАВА-КАЛЕНДАРЬ-УВЕДОМЛЕНИЯ.md | Права/график/уведомления, SEO-техника | GH ✅ · AM 📋 |
| 15 | 02.08 | ПРОМПТ-ОКНО-15-КАЛЕНДАРЬ.md | Календарь | GH ✅ · AM 📋 |
| 16 | 03.08 | ПРОМПТ-ОКНО-16-ГРАФИК-РАБОТЫ.md | Единый блок "График работы", `master_weekly_schedule` (миграция 022) | GH ✅ · AM 📋 (косвенно подтверждён - Окна 21/26 живьём зависят от этой схемы и работают) |
| 17 | 03.08 | ПРОМПТ-ОКНО-17-ГРАФИК-КОНТРАКТ.md | Контракт графика (schedule-range/DELETE) | GH ✅ · AM 📋 |
| 18 | 03.08 | ПРОМПТ-ОКНО-18-ГРАФИК-ВЛАДЕЛЕЦ-АДМИН.md | Неделя/Месяц/Год на реальных данных | GH ✅ · AM 📋 |
| 19 | 03.08 | ПРОМПТ-ОКНО-19-ГРАФИК-МАСТЕР.md | Read-only график мастера, фикс бага "Отпуск" | GH ✅ · AM 📋 |
| 20 | 03.08 | ПРОМПТ-ОКНО-20-ЛИМИТ-ЗАПИСИ-60-ДНЕЙ.md | Лимит публичной записи 60 дней вперёд | GH ✅ · AM 📋 |
| 21 | 04.08 | ПРОМПТ-ОКНО-21-КАЛЕНДАРЬ-РЕАЛЬНАЯ-ДОСТУПНОСТЬ.md | Грейаут недоступных дат до клика | GH ✅ · AM ✅ (`/schedule-availability` живой, не 404) |
| 22 | 04.08 | ПРОМПТ-ОКНО-22-СКРЫТЬ-МАСТЕРОВ-БЕЗ-ГРАФИКА.md | Мастер без графика скрыт из `/staff` | GH ✅ · AM 📋 (batch-деплой 04.08 ночь, подтверждено логом) |
| 23 | 04.08 | ПРОМПТ-ОКНО-23-ОТМЕНА-ОТГУЛА.md | Отмена одобренного отгула/отпуска целиком | GH ✅ · AM 📋 (роут PATCH-only, live-check этого аудита неубедителен из-за метода) |
| 24 | 04.08 | ПРОМПТ-ОКНО-24-ПРАЗДНИКИ-ПРОИЗВОДСТВЕННЫЙ-КАЛЕНДАРЬ.md | Производственный календарь РФ в БД + фикс отгулов | GH ✅ · AM ✅ (`/holidays` живой, 200) |
| 25 | 04.08 | ПРОМПТ-ОКНО-25-НАВИГАЦИЯ-ДЕНЬ-НЕДЕЛЯ-МЕСЯЦ-ГОД.md | Единое состояние даты между вкладками (чистый фронт) | GH ✅ |
| 26 | 04.08 | ПРОМПТ-ОКНО-26-КАРТОЧКА-МАСТЕРА-БЕЙДЖ-ДОСТУПНОСТИ.md | Бейдж ближайшей доступности мастера | GH ✅ · AM ✅ (`/masters-next-availability` живой, 200) |
| 27 | 04.08 | ПРОМПТ-ОКНО-27-КОМПАКТНЫЕ-ИКОНКИ-ГРАФИКА.md | Компактные иконки графика (чистый фронт) | GH ✅ |
| 28 | 04.08 | ПРОМПТ-ОКНО-28-МЕЛКАЯ-ПОЛИРОВКА.md | Полировка UI | GH ✅ · AM 📋 |
| 29 | 05.08 | ПРОМПТ-ОКНО-29-ИСПРАВЛЕНИЕ-ХВОСТОВ-АУДИТА.md | Хвосты первого продуктового аудита | GH ✅ · AM 📋 |
| 30 | 05.08 | ПРОМПТ-ОКНО-30-АУДИТ.md | Продуктовый аудит (породил 7 доков в `docs/archive/`) | - (аналитика, не код) |
| 31 | 05.08 | ПРОМПТ-ОКНО-31-ПОЛИРОВКА-ВНУТРЕННИХ-НАДПИСЕЙ.md | Убраны внутренние dev-надписи из CRM | GH ✅ |
| 32 | 05.08 | ПРОМПТ-ОКНО-32-UX-РЕДИЗАЙН-ГУГЛ-ЭППЛ-УРОВЕНЬ.md | UX-полировка до уровня Google/Apple | GH ✅ |
| 33 | 06.08 | ПРОМПТ-ОКНО-33-БЕЗОПАСНОСТЬ-KV-PIN-РЕЕСТР-РОУТОВ.md | Удалён эксплойт `/kv/:key`, default-deny реестр роутов | GH ✅ · AM ✅ (`/kv/test` подтверждённо 404 - эксплойт закрыт) |
| 34 | 06.08 | ПРОМПТ-ОКНО-34-СЕТЕВАЯ-ОШИБКА-ЗАПИСИ-КЛИЕНТА.md | Клиент не теряет запись при сетевой ошибке (чистый фронт) | GH ✅ |
| 35 | 06.08 | ПРОМПТ-ОКНО-35-АЛЕРТ-МАСТЕР-БЕЗ-ГРАФИКА.md | Алерт владельцу "мастер потерял график" | GH ✅ · AM 📋 |
| 36 | 06.08 | ПРОМПТ-ОКНО-36-ЧЕСТНЫЙ-ИНТЕРФЕЙС-ВЛАДЕЛЕЦ.md | Убраны нерабочие UI-элементы у владельца | GH ✅ |
| 37 | 06.08 | ПРОМПТ-ОКНО-37-ЕДИНЫЙ-РЕЗОЛВЕР-ЗП.md | Единый резолвер `computeMasterPayroll` | GH ✅ · AM ✅ (`/payroll` живой, 401) |
| 38 | 06.08 | ПРОМПТ-ОКНО-38-ВЫРУЧКА-ДНЯ-АДМИНИСТРАТОРУ.md | `computeRevenueToday`, карточка админу | GH ✅ · AM ✅ (`/revenue/today` живой, 401) |
| 39 | 06.08 | ПРОМПТ-ОКНО-39-КАРТОЧКА-КЛИЕНТА-РИСК-УХОДА.md | Карточка клиента, индикатор риска ухода | GH ✅ · AM ✅ (см. ⚠️ выше - лог был неверен) |
| 40 | 06.08 | ПРОМПТ-ОКНО-40-ДАШБОРД-ВЛАДЕЛЬЦА-СЕГОДНЯ.md | Дашборд владельца "Сегодня" + QA-логин | GH ✅ · AM - не проверено (нет однозначного роута под рукой) |
| 41 | 07.08 | ПРОМПТ-ОКНО-41-DESIGN-TOKENS-APP-SHELL.md | Design tokens + постоянный sidebar/topbar владельца | GH ✅ (см. ⚠️ выше - лог был неверен), бэкенд не нужен (чистый фронт) |
| Этап 1 | 07.08 | (план `plans/archive/`) | Декомпозиция `crm-auth.js` 1908→166 строк, 10 доменных файлов | GH ✅ |
| Этап 2 | 07.08 | (план `plans/archive/`) | Декомпозиция `api/server.mjs` 2719→559 строк, `lib/`+`routes/` | AM ✅ (по логу, 20/20 проверок на бою; не в `origin/main` - Amvera деплоится отдельным клоном, не через GH Pages, это ожидаемо) |
| 46 | 08.08 | (без ПРОМПТ-файла - прямой запрос в чате) | Кнопка "Обновить данные" (Окно 45) обновляла только "Расписание"/риск-список/заявки/бейдж; довела до всего кабинета владельца - "Команда" (разовые перерывы + недельный график каждого мастера, `assets/crm-schedule-editor.js`, `window.__refreshTeamSchedules`) и "Финансы" (выручка/ЗП дня, `refreshFinance()`, `assets/crm-dashboard.js`). "Аналитика" не тронута - там нет ни одного реального запроса (только "00% пример"), обновлять нечего | GH - не задеплоено, коммит локальный |
| 47 | 09.08 | ПРОМПТ-ОКНО-47-APP-SHELL-КОНТРАКТ.md | `assets/crm-app-shell.js` параметризован по роли (`ROLE_CONFIG`), Owner - без изменений поведения. `plans/2026-08-09-admin-master-app-shell.md`, Фаза 1 | GH ✅ (`a57a8e4` в `origin/main`, сверено 11.08) |
| 49 | 09.08 | ПРОМПТ-ОКНО-49-APP-SHELL-АДМИНИСТРАТОР.md | Sidebar у Администратора (2 пункта: Расписание/Сотрудники), убран дублирующий `<h1>`. Фаза 2 | GH ✅ (`02db3a7` в `origin/main`, сверено 11.08) |
| 50 | 09.08 | ПРОМПТ-ОКНО-50-APP-SHELL-МАСТЕР.md | Sidebar у Мастера (3 пункта: Мой день/Моя зарплата/Личные данные, новая иконка), убран дублирующий `<h1>`. Фаза 3, план закрыт | GH ✅ (`a3f552d` в `origin/main`, сверено 11.08) |
| 53 | 09.08 | ПРОМПТ-ОКНО-53-БАГИ-РАСПИСАНИЯ-ВЛАДЕЛЕЦ.md | 10 багов расписания владельца (A-J) + отдельно desktop-утечка дат `c59154b`. Хвост окна (счётчик неопознанных визитов) доехал только 11.08 - см. Окно 55, Задача E | GH ✅ (`30e01ac`, `c59154b`) |
| 54 | 10.08 | ПРОМПТ-ОКНО-54-КОНТРАКТ-ПОИСК-КЛИЕНТА-ПЕРЕНОС-ЗАПИСИ.md | Бэкенд-контракт под Окно 55: `GET /clients?phone=`, `PATCH /bookings/:id/reschedule`, уведомления `booking_moved_out`/`booking_moved_in`, `refresh`-режим в `notifyStaff`, миграции 041/042. Фронтенд не тронут ни строкой | AM ✅ (`3db9c9a`, `/reschedule` 404→401) · GH ✅ (`179c7a3`) |
| 55 | 10-11.08 | ПРОМПТ-ОКНО-55-OBEDINENIE-FORM-ZAPISI.md | Одна форма записи вместо двух (`#bd-1` удалён у owner/admin, у мастера оставлен намеренно - он записи не переносит), опознание клиента по телефону, режим `edit` (смена мастера/даты/времени/услуг через `/reschedule`), XSS-фикс в уведомлениях + иконки 📤/📥. Задача E - хвост Окна 53 доведён до прода в порядке бэкенд→curl→фронт. Живой CDP 33/33 | AM ✅ (`ce3a23c`, `/revenue/today` отдаёт `unidentifiedCount`) · GH ✅ (`3b31743`, `d451192`, build `d451192`) |
| 56 | 11.08 | (без промпт-файла - инцидент прав, устный запрос Влада) | Владелец сменил СЕБЕ роль на «мастер» в кабинете и запер систему: `PUT /staff/:id/role` доступен только owner, других владельцев в базе нет (вычищены миграциями 014/024/027/035/039). Побочный симптом - 403 от `GET /schedule-range` на чужих мастеров при открытой странице владельца. Миграция 043 возвращает роль, `isLastOwnerDemotion` отдаёт 409 вместо самозапирания. 6 юнитов + 12/12 живых HTTP-ассертов на эфемерной базе | AM ✅ (`2f5a4cf`, 200→503→200, смок 11/11) · GH ✅ (`9d348b2`, build `9d348b2`, ливом) |
| 59 | 20-21.08 | (без промпт-файла - устный запрос Влада) · `specs/2026-08-20-top-master-tarif.md`, `plans/archive/2026-08-20-top-master-tarif.md` | Топ-мастер по услуге. `master_services.is_top` + редактируемая цена в карточке сотрудника («Команда» владельца), `bookings.master_tier` фиксирует условия визита и пересчитывается при смене состава услуг и переносе. На сайте порядок шагов стал «услуги → тариф (обычный / топ, с надбавкой) → мастер», блок тарифа показывается только когда есть обе группы. В карточке записи метка «топ» и строка «Условия визита: канал · запись к топ-мастеру». Попутно: `PUT /master-services` валидирует цену (0/минус/дробь → 400), а `tools/verify-lib.mjs` с 18.08 не подменял `ALIKHAN_API_URL` - прогоны сайта молча ходили в боевой бэкенд. Живьём: 24/24 API, 18/18 CRM, 33/33 сайт, 509 офлайн-тестов | AM ✅ (`3f0555f`, 503→200 + `isTop` в `/public/masters`) · GH ✅ (`4c19428` + фикс нумерации шагов `5fabb26`, build подтверждён, живой сайт проверен) |
| 60 | 21.08 | (без промпт-файла - две находки Влада на проде) | (1) Декоративная окружность `.booking::before` не имела `pointer-events:none` и перехватывала клики по правой верхней карточке формы записи: услуга «Комплекс стрижка+борода» выбиралась, но не снималась. Пока услуги были вторым шагом, под кругом стоял правый верхний МАСТЕР - его клиенты тоже не могли выбрать. (2) Подсказка о связанной услуге при правке цены (комплекс ↔ его части) - система не пересчитывает цены друг из друга, а напоминает проверить связанную. Регресс-щит: хит-тест карточек в положении якоря `#booking`, проверено красным на версии без фикса | GH ✅ (`b297dfb`, `91fc992`, build подтверждён, перекрытие на живом сайте перепроверено - клики проходят) |
| 60 | 19.08 | (без промпт-файла - устный запрос Влада «CRM выглядит как у Тима Бёртона, сделай в стиле сайта, но не мрачной») | Тема «Дневной свет»: вес палитры перевёрнут (полотно = сайтовые чернила #f6f2e8→#EAE5D9, сайтовый графит уехал в sidebar/topbar), сама палитра не менялась. Отдельным слоем `assets/crm-theme-daylight.css` поверх всех CSS - ни один компонент не переписан, откат = снять один `<link>`. Золото разведено на текстовое (#8B6933) и заливочное (#C29A55) через локальную подмену переменной, без `!important`. Попутно найден и починен мёртвый путь к гербу-водяному знаку (`assets/assets/brand/...` → 404 с 18.08). Проверка `tools/verify-daylight-theme.mjs`: 11 разделов × 3 роли, 0 тёмных пятен, 0 текстов ниже WCAG AA. `node --test` 464/464 | **не задеплоено** (код локальный, деплой отдельной командой) |
| 61 | 20.08 | (без промпт-файла - устный запрос Влада про «Уведомления» и колокольчик) | Раздел «Уведомления» и колокольчик сведены к одному содержимому - записям клиентов, - и отличаются только подробностью: колокольчик даёт последние 6 строк и «показать все», раздел - карточку записи с «Открыть запись» (переход в «Расписание» на дату + штатное открытие карточки через новый хук `window.__openScheduleDay`) и связью с клиентом в один клик (WhatsApp/Telegram/СМС/звонок с готовым текстом; МАКС в этом списке невозможен - у мессенджера нет ссылки на чат по номеру). `GET /notifications` начал отдавать саму запись (дата/время/мастер/клиент/услуги), телефон - только ролям из `BOOKING_OPERATOR_ROLES`. Владелец наконец получает уведомления о новых записях (`bookingWatcherIds` вместо `locationAdminIds`). Снято: напоминания «за 15 минут»/«время пришло» вместе с фоновым сканером, весь механизм заявок мастера на отгул (форма, роуты `/schedule-requests`, `pendingRequests` в `/owner/alerts`, 3 модуля), уведомление «пропал график» (расчёт для баннера остался). Миграция 051 сузила CHECK до трёх записевых типов. Событие `notifications` в потоке событий впервые начало публиковаться - до этого тип был объявлен, но никем не отправлялся. Эмодзи-иконки ленты заменены на SVG набора. Живой CDP `tools/verify-2026-08-20-uvedomleniya-o-zapisyah.mjs` 37/37 (включая XSS в имени клиента и невидимость телефона для мастера), `node --test` 466/466 | **не задеплоено** (код локальный, деплой отдельной командой) |
| 62 | 21.08 | (без промпт-файла - список из 9 правок Влада по «Финансам») | Раздел «Финансы» владельца. **Главное:** в деньги идут только состоявшиеся визиты (`status='done'` - зелёная карточка в расписании). До этого выручка, зарплаты и «чистый доход» суммировали ВСЁ подряд, включая запланированные, неявки и отменённые брони - цифра была прогнозом, а выглядела как факт. Фильтр один на оба контура (`paidBookings` в `assets/crm-shared.js` + `AND status='done'` в `computeMasterPayroll`). Блок «Зарплаты мастеров» перестал быть тремя захардкоженными наборами разметки (`zp1/zp2/zp3`, `payrollMaster1..3`) - карточки строит `assets/crm-payroll-cards.js` по составу из `/staff`, поэтому новый сотрудник с включённым «Принимает клиентов» попадает туда сам. Поле «Ставка от выручки, %» теперь у КАЖДОГО, кто оказывает услуги (было только у Екатерины), у владельца и управляющего тоже - вместе с этим их зарплата перестала вычитаться из общей суммы жёстко в коде и входит в «Зарплаты мастеров» по той же формуле, что у всех. Незаданная ставка равна нулю у ЛЮБОЙ роли: процент - это доля выручки, которая уходит мастеру, и у владельца она по смыслу нулевая (его собственные стрижки не расход бизнеса, эти деньги остаются в «Чистом доходе»), а новому сотруднику её всё равно назначает Алихан сам. Прежнее «владельцу по умолчанию 100%» отменено второй правкой Влада в тот же день - он сам пересобрал смысл поля вслух и оказался прав. Аватар рядом с именем, как в остальных разделах. Убраны все пояснительные подписи и чип «реально»; подсказки о негодной ставке ушли во всплывающие сообщения. **Попутная находка:** `filterStaffForViewer` (`api/lib/schedule-core.js`) проверял роль литералом `'owner' || 'admin'` и не получил роль `manager` (Окно 57) - `GET /staff` молча вырезал у управляющего каждого сотрудника без рабочего дня в графике, то есть только что нанятый мастер был виден владельцу и невидим управляющему. Плюс `PUT /payroll-settings` начал валидировать диапазон 0-100 (раньше держал только CHECK таблицы → 500). Вторая волна правок в тот же день: карточки сотрудников положены на полотно раздела (`--bg-main`), а не на белое тело раскрытого родителя - сами карточки везде одинаковые (#FFFDF7 / #FFFFFF), отличалась подложка, из-за чего в «Финансах» они читались иначе, чем в «Дне» и «Команде»; кнопка «Развернуть все» стала раскрывать и вложенные карточки (знала только прямых детей `.staff-list`, поэтому блок раскрывался, а колонка карточек внутри оставалась закрытой), а вложенный `.staff-list` больше не получает вторую плавающую кнопку в тот же угол экрана. Третья находка Влада - календарь «Задать период» обрезался краем карточки: `details.staff-card` в owner-UI стоит `overflow:clip` (держит акцентную полосу `::before` в скруглениях), а `.custom-date-panel` это `position:absolute`, в поток не встаёт и карточку под себя не растит. Раньше не проявлялось - виджеты дат жили в одной высокой карточке со всеми мастерами сразу. Обрезка отпускается через `:has(.custom-date-panel:not([hidden]))` ровно пока календарь открыт; та же обрезка была и в «Команде» (разовые изменения графика), правило общее и чинит оба места. Живьём `tools/verify-2026-08-21-finansy-zarplaty.mjs` 47/47 (включая телефон 390px), `node --test` 526/526 | **не задеплоено** (код локальный, деплой отдельной командой) |
| 63 | 21.08 | (без промпт-файла - устный запрос Влада «новый пункт меню Клиенты между Командой и Финансами») | Раздел «Клиенты» у владельца и управляющего: база клиентов, собранная ИЗ ЗАПИСЕЙ, а не заведённая руками (кнопки «добавить клиента» намеренно нет - иначе появился бы второй способ завести человека и два ряда на одного). Новая ветка `GET /clients?all=true` одним агрегирующим запросом отдаёт имя, телефон, число состоявшихся визитов, сколько принёс, когда и откуда пришёл, число комментариев и последний из них; история конкретного клиента подгружается тем же `GET /clients/:id` в момент раскрытия карточки, а не всем списком сразу. Деньги считаются как в «Финансах» - списочная цена услуг визитов со статусом `done` (решение Влада: одно число в двух разделах, а не два расходящихся); `actual_price` на эту цифру не влияет. «Когда и откуда пришёл» - первая НЕотменённая бронь и её `client_source` (миграция 050); клиенты, записанные до появления этого поля, канала не имеют, и «Другое» им не подставляется. Клиент без телефона (walk-in) в базе не появляется вовсе - система намеренно не связывает такие визиты между собой (решение Алихана, Окно 53), и это сказано строкой под списком, а не умолчанием. Поиск по имени и по последним цифрам телефона фильтрует уже загруженный список, без запроса на каждую букву. Права: `?all=true` только `MANAGEMENT_ROLES` (403 администратору даже прямым запросом мимо интерфейса), мастеру в карточке клиента срезается не только комментарий, но и цена визита вместе с итоговой суммой. **Комментарий к записи** вырос с 500 до 3000 знаков (требование заказчика) и стал `textarea` на обеих страницах; смысл поля расширен - это заметка не только про сумму, но и про клиента, она видна в его карточке спустя месяцы. Длинные комментарии в истории свёрнуты («Показать целиком») - иначе одна заметка выталкивала всю историю за экран. Попутно: `details.staff-card summary` (mockup-crm.css) раскладывал во flex ЛЮБОЙ вложенный `summary`, из-за чего подпись свёрнутого комментария уезжала вправо - найдено живым замером `getBoundingClientRect`, не на глаз. Проверен вопрос Влада «а если оплату поставили неверно и потом переключили визит на неявку»: цифры нигде не замораживаются - и «Финансы», и итоги клиента считаются на лету по визитам `done`, поэтому смена статуса убирает деньги из обеих цифр и возвращает их при откате статуса; комментарий и вписанная сумма при этом остаются на записи (объяснение, почему так вышло, не должно исчезать вместе со статусом). Правка Влада сразу после деплоя: в разделе не было кнопки «Развернуть все» - общий механизм `initCrmNavigationPanels` отрабатывает на загрузке страницы, а карточек клиентов тогда ещё нет (список приходит при первом заходе в раздел), поэтому кнопка не создавалась; зовём механизм после отрисовки списка. Заодно история визитов грузится очередью по 4 - «Развернуть все» на живой базе иначе дало бы сотню одновременных запросов. Вторая волна правок Влада по живому разделу: статусы визитов в истории получили цвет тем же языком, что карточки в календаре «Дня» (зелёный обслужен / красный не пришёл / золото ожидается / серый отменена - полоса слева + плашка статуса), «с 17.08.2026» стало «с нами с 17.08.2026», шапка карточки клиента перебрана в набор плашек (число визитов, деньги акцентом, «с нами с», канал, красная плашка риска с точкой-маркером), у клиента без визитов вместо «0 визитов, принёс 0 ₽» одна плашка «Пока без визитов», строка-пояснение про walk-in убрана. Попутная находка прогона: `initCrmNavigationPanels` при перерисовке списка (поиск фильтрует карточки) не пересинхронизировал подпись кнопки - над свежим свёрнутым списком висело «Свернуть все»; теперь уже подключённый список пересинхронизируется, а не пропускается. Жалобы Влада с прода в тот же день - «не заходит в CRM, обновил несколько раз и тогда зашло» и «клиенты очень долго грузятся». Замер: сам раздел быстрый (список 66-74 мс, карточка 5-8 мс на базе 3000 клиентов / 15000 записей, индекс `bookings_client_history_idx` уже есть), но (1) список тянул ПОЛНЫЙ текст последнего комментария каждого клиента - ответ 9,3 МБ; обрезан до 120 знаков → 1,08 МБ и вдвое быстрее, целиком комментарий отдаёт карточка; (2) корень «не заходит» - спячка контейнера Amvera: монитор /health за 5 минут поймал ответ 17 000 мс и полный обрыв по таймауту при обычных 170 мс. Лечится не кодом, но кодом смягчается - `fetchWithWakeup` (assets/crm-shared.js) повторяет ТОЛЬКО безопасное (обрыв связи и 502/503/504, паузы 1.5 с и 4 с, максимум 3 попытки) и показывает «Сервер просыпается…» под кнопкой входа; 4xx не повторяется никогда (повтор с неверным PIN выглядел бы как подбор пароля), POST/PUT/DELETE не повторяется (запрос мог дойти и выполниться - повтор завёл бы вторую бронь). 7 офлайн-тестов на решение о повторе. **Находка, к разделу отношения не имеющая, но важная:** `tests/api.roles.test.js` и `tests/storage.media-url.test.js` ходили в БОЕВОЙ API (первый ещё и писал в живую базу - создавал сотрудников и брони, потом удалял) и попадали в каждый обычный `node --test`; в мониторинге прода 21.08.2026 видны ответы 429 «too many requests» ровно в моменты прогонов. Переименованы в `*.live.mjs` - из автопрогона выведены, запускаются осознанно (записано в CLAUDE.md, COMMANDS). Живьём `tools/verify-2026-08-21-klienty.mjs` 40/40 (включая 3000 знаков в базе и на экране, 3001 → 400, 403 администратору, отсутствие walk-in в базе, done→no_show→done на живой «Выручке за год»), `node --test` 537/537 | **не задеплоено** (код локальный, деплой отдельной командой) |
| 64 | 21.08 | (без промпт-файла - две устные правки Влада: превью ссылки и переход из истории клиента) | **(1) Превью ссылки.** При пересылке сайта или CRM в мессенджер показывалось фото фасада (`assets/og-cover.jpg`) - теперь фирменная карточка с логотипом на тёмном полотне сайта (`assets/og-brand.jpg`, 1200x630, собрана из `assets/brand/lockup-footer.webp`). Новое имя файла, а не подмена старого: мессенджеры кэшируют превью по URL картинки. У CRM-страниц (`crm-owner/admin/master.html`) og-тегов не было вовсе - при пересылке ссылки на кабинет превью не появлялось; добавлены вместе с `robots: noindex` (кабинет в поиске не нужен). `schema.org` `image` остался фотографией заведения - там это про сам барбершоп, логотип уже отдельным полем `logo`. **(2) Переход в запись из истории клиента.** Строка визита в карточке клиента («19.08.2026 15:00 · не пришёл · Стрижка · Алиовсад») стала кнопкой: клик уводит в «Расписание» → «День» на дату визита и открывает саму запись, тем же путём, что кнопка «Открыть запись» в уведомлениях (`openBookingFromNotification` экспортирована из `assets/crm-notifications.js` - двух реализаций одного перехода не заводим). Отменённый визит кнопкой НЕ становится: в календаре у такой брони нет карточки с `data-id` (`buildCancelledCard`), проваливаться некуда - вместо этого подсказка почему. Клик по «Показать целиком» внутри визита раскрывает комментарий и в расписание не уводит. Попутно у самого перехода убрана гонка: вход в «Расписание» сам грузит СЕГОДНЯШНИЙ день (`raiseDayOnEnter`), и для записи на другую дату его ответ мог прийти последним и затереть нужный день - теперь несколько попыток с проверкой, что карточка появилась. **(3) Связь с клиентом** рядом с «Записать снова»: вместо одинокой «Позвонить» тот же ряд, что в уведомлениях - WhatsApp / Telegram / СМС / Позвонить. Текст подставляется, только если у клиента есть ближайшая запланированная запись (напоминание о ней); если её нет - открывается пустой чат, а не выдуманное приглашение от лица барбершопа. **(4) MAX в ряду связи** (правка Влада сразу после деплоя: «а МАКСа нет! добавь MAX и в уведомления тоже») - и в карточке клиента, и в ленте «Уведомлений». Работает не как остальные три: ссылки на чат по номеру у MAX не существует (проверено 21.08.2026 по справке help.max.ru, не по памяти - это решение мессенджера ради приватности; отправка ботом по номеру требует корпоративной регистрации бота и согласия клиента, выданного заранее именно ему). Поэтому кнопка открывает `web.max.ru` и кладёт номер в буфер, а тост говорит вставить его в штатный поиск «Найти по номеру» - два шага вместо набора номера руками. Разметка ряда вынесена в `messengerButtonsHtml`, чтобы кнопки не разошлись между разделом клиентов и лентой. Живьём `tools/verify-2026-08-21-perehod-iz-istorii-klienta.mjs` 18/18, `node --test` 535/535 | **не задеплоено** (код локальный, деплой отдельной командой) |
| 65 | 21.08 | (без промпт-файла - устный запрос Влада со скриншотами Yclients от заказчика: «Неделя» и «Месяц» неудобны Али) | **«Неделя» и «Месяц» переделаны в один компонент «график работы»** - матрица мастера × даты (`assets/crm-schedule-matrix.js`), у которой меняется только ширина окна дат: 7 колонок в «Неделе», весь месяц с горизонтальным скроллом в «Месяце». Причина - на присланных экранах Yclients недели-как-своего-экрана нет вовсе: есть график команды и полоска дат для прыжка по дням. Ушли вместе с причиной существовать: сетка 7 карточек-колонок за ОДНОГО мастера (Окно 44), календарные квадратики 7×N, переключатели `#weekMasterSwitch`/`#monthMasterSwitch` (матрица показывает всю команду сразу) и режим «Все мастера / по одному» - агрегатного % команды на день больше нет, в каждой ячейке честный % конкретного мастера. Колонка имён приморожена при скролле (`position: sticky`), клик по дате в шапке уводит в «День», клик по ячейке открывает правку графика ИМЕННО того мастера, чья это строка (`openDayEditModal(date, masterId)` - раньше мастер брался из общего состояния, которое выставляли пилюли). **Полоска дней недели под «Днём»** (`assets/crm-schedule-daystrip.js`) - второе прямое требование со скриншота: на телефоне пристыкована к низу экрана, на десктопе обычная строка. Две находки живого прогона, которые чтением статики не берутся: (1) `position: sticky; bottom: 0` не липнет - полоска стоит последней в панели «Дня», прилипать не к чему (замер: `bottom` 387px при экране 844px), а `position: fixed` без переноса в `<body>` считает «низ экрана» от анимированного предка с `transform` (замер: `bottom` -626px) - поэтому стыковка это класс `.is-docked` + перенос узла; (2) тело свёрнутой карточки `<details>` в этом проекте НЕ исчезает из потока (своя анимация раскрытия), `getClientRects()` у него живые и высота 795px - «виден ли День» пришлось решать парой «карточка раскрыта И раздел показан». **Лента месяцев** (`#monthStrip`, «Июль Август Сентябрь…») вместо N нажатий на стрелку. Глазами на скриншотах прогона поймано ещё два дефекта, мимо которых автоматика прошла зелёной: часы `10:00–20:00` обрезались многоточием на узкой колонке (перенос на две строки), плавающая кнопка «Развернуть все» ложилась поверх полоски (поднимается, пока полоска пристыкована), а легенда обещала три цветных точки, которых в матрице нет (заменена образцами самой ячейки - полоса слева / пунктир). Мастер (`crm-master.html`) видит только свою строку и график не правит - ячейки не кнопки. Бэкенд не менялся: матрица собирается из тех же `/schedule-range`, `/master-weekly-schedule` и одного `/bookings` на диапазон. **Правка того же дня (Влад: «сделай неделю и месяц ближе по дизайну ко дню»):** матрица переведена на язык вида «День» - ячейка это тот же трек мастера (радиус 10px, фон `--surface-2`, рамка `--border`), выходной - та же диагональная штриховка на `--panel-bg-collapsed` с подписью курсивом (`.schedule-track.day-off`), мастер без графика - сплошной `--surface-3` со словами «нет графика» (`.no-schedule`, путать эти два состояния нельзя - Окно 43), разовая правка - полоса слева 4px как маркер статуса у карточки записи (`.appt--status-*`), загрузка занятого дня - плашка в цвете «ожидается» (`.appt--new`, `#F3DCAB`), hover поднимает ячейку той же тенью, что `.appt:hover`. Находка живого замера: класс плашки сначала назвали `.is-busy` - имя ЗАНЯТО глобальным состоянием «кнопка ждёт сервер» (`assets/crm-loading.css`: `color: transparent !important` + спиннер `::after`), и вместо «10% · 1 зап.» на экране был золотой овал со спиннером (`getComputedStyle` дал `color: rgba(0,0,0,0)`); переименован в `.sm-cell-load--busy`, закрыт офлайн-тестом. **Третья волна правок Влада по живому графику (21.08.2026):** (1) колонка сотрудников - кружок и имя ПОД ним, как в Yclients, без плашки-подложки: имя строкой справа не помещалось ни на одной ширине; фон примороженной колонки взят от самой карточки раздела (`--owner-panel-active`) - замер живьём дал у карточки `rgb(255,255,255)` против `rgb(247,243,233)` у колонки, отсюда и «блок за кружочком». (2) Цвета выходного и фона ячеек не совпадали с «Днём», потому что светлая тема переопределяла `.schedule-track` ЛИТЕРАЛАМИ, которых ячейка не видела (`#FFFDF7` у дорожки против `#F7F3E9` у ячейки). Заведены переменные `--track-bg` / `--track-off-bg` / `--track-off-stripe` / `--track-none-bg` - одна точка правды на «День» и график, тема переопределяет их, а не каждое место; закрыто офлайн-тестом (`tests/schedule-track-colors.test.js`) и живым сравнением вычисленного цвета. (3) Сетка стала просторнее: колонка 124px, ячейка от 78px, крупнее часы и числа дат. (4) Легенды над сеткой убраны по просьбе Влада вместе со стилями - признаки читаются с самой ячейки. **Регрессия, найденная глазами на снимке:** подпись карточки говорила «Месяц · Август», а сетка под ней показывала сентябрь - якорь рисуется из общей даты на каждый `setView` (Окно 25), а данные вида перечитывались только когда он активен. Теперь при смене даты соседние виды перечитываются, но только если реально сменился ПЕРИОД (другая неделя / другой месяц); свёрнутая карточка помечается устаревшей и дочитывается при раскрытии. **Четвёртая волна правок Влада (21.08.2026, «так минималистичнее»):** лента месяцев удалена, показанный период стоит МЕЖДУ стрелками навигации (`#weekNavLabel` «17.08-23.08», `#monthNavLabel` «Август 2026») - ровно как дата во вкладке «День»; дублирующие подписи-якоря в шапках карточек («Неделя · 17-23 августа», «Месяц · Август 2026») убраны вместе с веткой их рендера. Подпись месяца над полоской дней переведена в именительный падеж («Август 2026», «Июль-Август 2026» вместо «августа 2026»). Полоса горизонтальной прокрутки на маке висит ПОВЕРХ содержимого и накрывала нижний ряд ячеек (снимок Влада) - под сеткой заложено 16px. **Пятая волна (21.08.2026, «колонки набиты кучей инфы»):** в ячейке остались ровно два носителя смысла - часы смены и плашка занятости; убраны строка перерыва (всё равно обрывалась многоточием «перерыв 13:00…») и «0%» у дней без записей (ноль повторялся в каждой второй ячейке). Данные не потеряны: перерыв и загрузка пустого дня перешли в подсказку ячейки (`title`) и в редактор дня, это отдельно проверяется живым прогоном. Запас под полосой прокрутки увеличен до 26px. Живьём `tools/verify-2026-08-21-okno65-grafik-raboty.mjs` 32/32, `node --test` 554/554 | GH ✅ (`398a993`, build `398a993` = built, живой curl: `assets/crm-schedule-matrix.js` 200, `crm-owner.html` содержит `#dayStrip` и `#monthStrip`) · AM - не требуется (бэкенд не менялся) |
| 66 | 21.08 | (без промпт-файла - жалоба Влада «когда грузишь фото профиля, кнопка "Сохранить изменения" не становится доступной») | **Диагноз оказался не тем, чем выглядел.** Живой прогон (`tools/verify-2026-08-21-foto-profilya.mjs`) показал: фото профиля УХОДИТ на сервер сразу после кадрирования, отдельным запросом `POST /staff/:id/media`, и кнопке «Сохранить изменения» тут делать нечего - она отправляет ПОЛЯ карточки. Подтверждено и на проде: у `master-1` в `GET /public/masters` стоит `photoUrl` - то самое фото, которое Влад считал непринятым. Настоящая поломка была в обратной связи: подпись «Фото профиля обновлено» писалась в `[data-card-note]`, который строкой ниже затирала `renderTeam()` (замер после загрузки давал пустую подпись). На экране человек видел ровно ничего - серую кнопку и молчание, отсюда вывод «не сработало». Починено: сообщение уходит в тост (переживает перерисовку, тот же приём, что у `savePin`) и заново ставится в подпись уже НОВОЙ карточки после `renderTeam`; в блоке «Фото профиля» прямо сказано, что оно сохраняется сразу и кнопка для этого не нужна - иначе несогласованность остаётся: удаление фото, наоборот, ждёт «Сохранить изменения» (решение Влада 15.08.2026). Попутная находка прогона: сервер по умолчанию пишет картинки в `/data/staff-media` (том Amvera), которого на локальной машине нет - без `STAFF_MEDIA_ROOT` любой локальный прогон загрузки красится в FAIL по инфраструктуре, а не по коду. Живьём 10/10, `node --test` 554/554 | **не задеплоено** на момент записи |
| 59 | 22-23.08 | брифы/ПРОМПТ-ОКНО-59-NEDOPOLUCHENNAYA-PRIBYL.md | **«Недополученная прибыль» - главная ценность продукта, а не учёт.** Миграция 056: срок возврата клиента - договорённость мастера с клиентом (`renew_days`), срок, который мастер считает правильным (`renew_days_recommended`), причина из закрытого словаря, кто и когда поставил. Закрыть визит клиента с телефоном без срока НЕЛЬЗЯ (проверка по состоянию базы, не по тому, что прислал фронт); «не обсуждали» = месяц по умолчанию и это законный ответ; walk-in без телефона поле не спрашивает; откат статуса срок не стирает. Возвращаемость и список невернувшихся перешли с общего `RETURN_GRACE_MONTHS = 1` на срок конкретного клиента - одна формула на обе цифры. Новое в «Финансах»: карточка с тремя суммами (отвал и неявки - ПОТЕРЯ, разрежённость - ПОТЕНЦИАЛ, не потеря) и списки «кому звонить» / «кому объяснить срок». В «Аналитике» - доля обсуждённых сроков по мастерам. Цена визита вынесена в общий `api/lib/pricing.js` (одна формула с зарплатой). Правка Влада по ходу окна: **статус визита ставит администратор/владелец/управляющий, роль master роут больше не пускает вовсе** - отметка «Обслужен» идёт в выручку и в ЗП того же мастера. Найдено живым прогоном и исправлено: карточка за «Месяц» прятала самых потерянных клиентов (считала только тех, у кого визит внутри окна). 604 офлайн-теста, 41/41 живых проверок | AM ✅ (`aff791e`, `/finance/missed-profit` 404→401, 503→200) · GH ✅ (`719579e`, build success, `assets/renew-reason.js` 200 ливом) |

| 67 | 24.08 | plans/2026-08-24-multitenancy-etap-a.md · specs/2026-08-24-multitenancy.md | **Мультиарендность, Этап A: изоляция арендаторов.** Одно ядро обслуживает несколько независимых бизнесов - подключение второго клиента (ортодонт Карина) стало строкой в справочнике, а не переделкой. Пять фаз. **(1)** Арендатор живёт в `AsyncLocalStorage`, `pool` в `lib/db.js` стал прокси и отдаёт соединение текущего запроса с уже выставленным `SET LOCAL app.tenant_id` - **ни одно из 222 мест с SQL не переписано**, вложенные `BEGIN` роутов подменяются точками сохранения в одном месте. Долгие ответы (поток событий, медиа) соединение не держат. **(2)** Миграция 057: справочник `tenants` + `tenant_id` во всех **20** таблицах данных (не 14, как считало исследование - список теперь вычисляется тестом из всех миграций), составные ключи вместо глобальных, индексы с арендатором впереди. **(3)** Миграция 058: `ENABLE` и `FORCE ROW LEVEL SECURITY` + политика на чтение и запись; миграция ОТКАЗЫВАЕТСЯ работать под суперпользователем и `BYPASSRLS` - иначе замок выглядит поставленным, но не держит. **(4)** Арендатор по домену запроса (`Origin`, не `Host` - API у арендаторов один), неизвестный домен → 404, CORS из справочника, вход по паре арендатор+почта. **(5)** Путь отката (`tools/rollback/`) проверен накатом-откатом-накатом со сверкой слепка схемы. **Находки живых прогонов, которых не было бы без них:** `DROP CONSTRAINT` молча не снимает уникальный ИНДЕКС (`clients_phone_key`); составные ключи ломают шесть боевых `ON CONFLICT`; одновременные запросы внутри одного запроса наезжали друг на друга на общем соединении (лечится очередью); ответ уходил клиенту ДО фиксации транзакции (лечится буфером - иначе кабинет получает «готово» на записи, которой в базе ещё нет). Изоляция доказана атакой на базе, где таблицы принадлежат обычной роли: со снятым `FORCE` утекают 20 таблиц из 20. Прогон 8 кабинетов (2 арендатора × 4 роли) по 22 роута - ни следа чужих данных. 646 офлайн-тестов, 7 живых инструментов | AM ✅ (`3a773f2`, живьём: неизвестный домен 404 `unknown_tenant`, домен Алихана 200 с данными, `dbRoleSafe: true`, три кабинета и публичный сайт проверены в браузере) · GH ✅ (`6b0811f`, переводы новых кодов ошибок) |
| 68 | 24.08 | брифы/ПРОМПТ-ОКНО-68-ETAP-B-SLOVAR-VERTIKALI.md · plans/2026-08-24-multitenancy-etap-b-slovar.md · specs/2026-08-24-multitenancy-etap-b-slovar.md | **Мультиарендность, Этап B: словарь вертикали и флаги разделов.** Один и тот же экран говорит словами своей вертикали: у Алихана «мастер / запись / услуга / клиент / салон», у клиники «врач / приём / процедура / пациент / клиника». **Задача оказалась не про замену слова.** В кабинетах живут 39 падежных форм («записи» 235 раз, «мастера» 168), а род у пар не совпадает: запись женского рода, приём мужского; салон мужского, клиника женского - наивная подстановка даёт «Новая приём». Отсюда двухслойный словарь: формы существительных (5 сущностей × 12 форм × 2 вертикали) плюс ~120 фраз, где рядом стоит согласуемое слово, с подстановкой `{имя}` для живых данных. Истина - `api/lib/vertical-terms.js`, кабинеты забирают её роутом `GET /tenant/appearance` по своему домену и своей копии не держат (запасные барбершопные слова во фронте пересобираются скриптом и сверяются тестом). Незнакомая вертикаль откатывается в барбершоп на каждом уровне: нет вертикали → барбершопный словарь, нет термина → барбершопный термин, нет формы → барбершопная форма, нет ключа нигде → сам ключ, чтобы пропуск было видно. **Флаги разделов** (`payroll`, `missedProfit`) - значение в справочнике арендаторов (миграция 060), проверка в реестре роутов рядом с правами: выключенный раздел отдаёт 404 ДО обработчика, а не прячется в меню. Ввод срока возврата флагом НЕ выключается - без него визит не закрыть, флаг режет отчёты, а не ввод. **Два сита на пропущенные места:** греп по исходникам с базой-храповиком (225 строк → 0) и скринер видимого текста в браузере на арендаторе-клинике (22 слова → 0, плюс встречная проверка, что клинические слова на экране ЕСТЬ - «ноль барбершопных» выполнимо и на пустом экране). **Главная находка:** словарь нельзя звать при загрузке модуля - константа вычисляется раньше, чем слова приедут с сервера, и врач до перезагрузки читает «мастер»; все такие таблицы переведены в функции, каркас пересобирает меню по событию прихода слов, заведены два теста на этот класс. **Найдено и починено по дороге, не в продукте:** прогон кабинетов молча выбирал невидимую кнопку «Развернуть все» (их несколько, все `position:fixed` в одном углу; `innerText` скрытого элемента в Chrome всё равно возвращает текст, а `offsetParent` у `position:fixed` всегда null) - из-за этого сверялся вдвое более бедный экран, и я успел сделать ложный вывод «кабинеты администратора и мастера пустые». Регресс доказан сверкой всего видимого текста трёх кабинетов с доокошечным коммитом: 2716 / 2448 / 1192 знака, различий строк ноль. 715 офлайн-тестов, 13 живых проверок словаря и флагов на настоящем Postgres, все пять прогонов Этапа A целы | AM ✅ (`8904900`, живьём 17 проверок: `/tenant/appearance` отдаёт барбершопный словарь по домену Алихана, неизвестный домен 404, POST 404, флаги `{payroll:true,missedProfit:true}`, `dbRoleSafe:true`; миграция доказана роутом, читающим новую колонку, а не health=200) · GH ✅ (`4a2dff4`, build подтверждён; кабинет владельца на проде открыт в браузере - меню «Клиенты», реальная команда и расписание, ноль ошибок в консоли, ни одного чужого слова). Копия базы снята перед миграцией: `alikhan-2026-08-26-1824.json`, 336 строк, 20 таблиц |
| — | 24.08 | (инцидент того же дня) | **Поток живых событий выключен по умолчанию.** Сразу после переключения кабинет входил, но оставался под шторкой: 5-6 запросов из 20 висли без ответа. Причина - не мультиарендность: при живом SSE **следующий** запрос по тому же соединению виснет («до потока 200/200/200, первый при потоке ТАЙМАУТ, следующие 200/200/200»), с заблокированным `/events` кабинет грузится полностью. Локально, без прокси Envoy, тот же код тем же браузером не воспроизводится - значит дело в прокси Amvera. Живые обновления идут штатным фолбэком (опрос `GET /changes`), разница для человека - обновление раз в несколько секунд вместо мгновенного. Включить обратно: `LIVE_EVENTS=on` в панели. Причину на стороне прокси ещё предстоит разобрать - поток живёт на проде с 17.08 и мог вести себя так же незаметно | AM ✅ (`3a773f2`) |
| 70 | 28.08 | tasks/konveyer/2026-08-27-alikhan-sbros-testovyh-dannyh/ (в ~/IvanOS) · api/lib/reset-tenant-data.js · tools/verify-2026-08-27-sbros-dannyh.mjs · tools/dry-run-sbros.mjs | **Сброс рабочих данных арендатора перед передачей заказчику.** Разовый механизм по образцу заведения арендатора: переменная окружения плюс перезапуск, одна транзакция в контексте арендатора, снимок отката в `kv_store` до первого удаления, идемпотентность по метке, два рубежа защиты соседа (сверка имени плюс белый список вертикали). Очищаются 8 таблиц рабочих данных, сразу за этим всем в штате пишется график 08:00-20:00 с перерывом 13:00-14:00 на 7 дней, строки уволенных снимаются. Аналитика производная, отдельной чистки не требует. Отдельной переменной `RESET_TENANT_DATA_DROP_SNAPSHOT` снимается снимок после подтверждения первого дня. Плюс фикс: секция «Услуги и время» в карточке сотрудника показывается только тому, кто принимает клиентов - у администратора она больше не рисуется. | **Прод: да.** Бэкенд Amvera `79d3e0e`, фронт GitHub Pages `1ae3f13`. Копия до операции - 342 строки, доказана восстановлением 342 из 342 в чистую базу под обычной ролью. Сброс 28.08 07:13:05Z: bookings 50→0, clients 30→0, notifications 65→0, booking_services 64→0, schedule_shifts/breaks/change_requests →0; staff 7, services 8, master_services 31, master_weekly_schedule 35, holidays 15, staff_media 4 не тронуты. У арендатора 2 (клиника Карины) изменённых таблиц 0. Разовые переменные убраны коммитом `4a54237`, `/backup` снова отдаёт 404 со старым токеном и ролью владельца. |
| 71 | 28.08 | api/lib/reset-tenant-data.js · tests/api.purge-staff.test.js · tools/verify-2026-08-28-purge-staff.mjs | **Удаление тестовых сотрудников перед передачей заказчику.** Замечание владельца: «Тест Аудит» и «Тест Сценарии» - не уволенные люди, а следы разработки, и в кабинете они видны разделом «Уволенные (2)». Разовая операция переменной `PURGE_STAFF` по канону сброса: снимок отката в `kv_store` до удаления, идемпотентность по метке, сверка имени и вертикали. Три предохранителя: только уже уволенные, только при нуле записей за ними, и только когда КАЖДЫЙ названный идентификатор есть в базе - иначе отказ целиком, не тронув никого. Репетиция на копии боевой базы поймала два дефекта до прода: `schedule_breaks` привязана к смене, а не к мастеру, и снимок хранил проекцию строки сотрудника вместо полной строки. | **Прод: да.** Бэкенд `79a7f12`, фронт `8ed9f67`. Выполнено 28.08 09:17:10Z: состав 7 → 5 человек, все в штате, раздел «Уволенные» из кабинета исчез. Переменная убрана коммитом `d80aaa9`. Тестов 803, репетиция - 19 проверок. |
| 72 | 28.08 | api/lib/login-throttle.js · api/routes/auth.js · api/routes/staff.js · assets/crm-auth.js · assets/crm-team.js · assets/crm-toast.js · crm-admin.html · crm-master.html · tools/okno72-loginy.mjs · tools/verify-2026-08-28-okno72-vhod.mjs · tests/api.login-throttle.test.js | **Вход по логину и паролю вместо «Email» и «PIN» перед передачей заказчику.** Замечание владельца: на входе стоит `master1-test@alikhan.test` - слово «тест» в боевой системе, домен `.test` зарезервирован под тесты и письмо на него не дойдёт, а логины не соответствуют людям (владелец заходил как «master1», администратор - как «master4»). Логин стал именем латиницей (`normalizeLogin`, 3-32 знака), прежние значения-адреса остались валидными - тот же движок обслуживает клинику Карины. Секрет из «ровно шесть цифр» стал паролем от шести знаков любыми символами (`isValidSecret`), правило одно для всех ролей. Роут входа принимает и новые ключи `{login, password}`, и старые `{email, pin}` - вкладка, открытая до выкатки, не ломается. Добавлен счётчик неудачных попыток (`login-throttle.js`): пять промахов подряд по паре «логин + адрес» - пауза 15 минут, ответ 429; до этого окна перебора не мешало ничто, а у владельца стоял четырёхзначный код. Отдельно: боевые пароли всех пятерых сотрудников лежали открытым текстом в 12 служебных скриптах публичного репозитория - вычищены, доступы читаются из окружения. По вопросу владельца («старый вход точно закрыт?») найден и закрыт ещё один дефект: смена пароля не обрывала прежние сессии, хотя интерфейс обещал «войти по старому уже нельзя» - вкладка со старым паролем жила бы ещё 30 дней. Теперь входы сотрудника обрываются, кроме текущей сессии того, кто задаёт пароль. | **Прод: да.** Бэкенд Amvera `6ab7378` (перезапуск 28.08 11:50:45Z), фронт GitHub Pages `f8c5e8c`. Переименование выполнено `tools/okno72-loginy.mjs --apply`: пятеро сотрудников переведены на логины aliovsad / mamedhan / elizaveta / admin / renat, всем заданы новые пароли. Живая проверка после выкатки: все пять новых входов отдают 200 с верной ролью, все пять старых адресов - 401; роли, точка и признак «принимает клиентов» у всех сохранены, услуг 8; вход под `aliovsad` в реальном браузере на боевом сайте открывает кабинет владельца (снимок `tools/shots-2026-08-28-okno72-prod.png`). Сайт клиники Карины отвечает 200. Тестов 813. |
| 73 | 28.08 | api/lib/webpush.js · api/lib/push-delivery.js · api/routes/push.js · api/migrations/061_push_subscriptions.sql · assets/crm-push.js · sw.js · manifest-{owner,admin,master}.webmanifest · assets/brand/icon-{192,512}.png · tests/api.webpush.test.js · tests/api.push-wiring.test.js · tools/verify-2026-08-28-okno73-push-ui.mjs | **Уведомления на телефон, а не только под колокольчиком.** Вопрос владельца: «почему нельзя всплывашками на телефонах?». Оказалось - можно, просто не было сделано. Реализован web push целиком на встроенном node:crypto, без единой новой зависимости: шифрование содержимого по RFC 8291 (ECDH P-256, HKDF, AES-128-GCM) и подпись отправителя по RFC 8292 (ES256). Подписки устройств - своя таблица под замком арендатора, повторная подписка обновляет строку и перепривязывает её к вошедшему (общий планшет администратора). Отправка вплетена в notifyStaff: что попадает в колокольчик, то попадает и на телефон, дубль по ON CONFLICT не звонит второй раз. Кабинеты получили описание для установки на экран телефона (по одному на роль) и фоновый обработчик, который намеренно ничего не кэширует. На айфоне вместо неработающего тумблера показывается инструкция: Apple разрешает уведомления только сайту, добавленному на экран Домой. Два дефекта пойманы до прода: фоновая отправка сначала работала по соединению завершённого запроса (соединение принадлежит запросу и отпускается вместе с ним) - теперь фон открывает свой контекст арендатора; тексты карточки были написаны барбершопными словами мимо словаря вертикали - переведены на словарь, для клиники это «приём». | **Прод: да.** Бэкенд Amvera `93e8bca` (код) и `74386b4` (ключи отправителя в конфиге запуска, перезапуск 28.08 15:48:29Z), фронт GitHub Pages `60b8ea0`. Живая проверка: `GET /push/key` отдаёт `configured: true` всем трём ролям, таблица подписок создана (`/push/status` отвечает `devices: 0`), карточка в кабинете владельца на боевом сайте показывает рабочую кнопку «Включить уведомления». Ключи лежат вне обоих репозиториев (`~/.alikhan-vapid-keys.json`) и в конфиге приватного репозитория Amvera - в публичный GitHub не уходят. Тестов 829, живой прогон интерфейса 14 из 14. Настоящую доставку headless-браузер проверить не может - ждёт подтверждения с живого телефона. |
| 74 | 28.08 | assets/crm-app-shell.js · assets/crm-team.js · assets/crm-schedule-views.js · assets/crm-schedule-view-{week,month}.js · api/routes/schedule.js · crm-admin.html · crm-master.html · tests/api.schedule-owner-only.test.js · tools/verify-2026-08-28-razdel-uvedomleniy.mjs | **Три правки владельца подряд.** (1) Убраны подписи «Себя уволить нельзя - это закрыло бы вам вход в CRM» и «Владельца уволить нельзя»: запрет очевиден без слов, а надпись читалась как отказ там, где человек ничего не пытался сделать. Сам замок на сервере не тронут. (2) График работы теперь меняют только владелец и управляющий - четыре точки изменения (разовые исключения, создание и удаление смены, недельный график) переведены с BOOKING_OPERATOR_ROLES на canManageStaff, вместе с ними убраны ставшие недостижимыми ветки проверки точки администратора. Чтение графика администратору осталось: без него он не сможет работать с записями. Интерфейс приведён в соответствие - матрица недели и месяца больше не редактируемая, модалка правки дня не открывается, а из карточки сотрудника у администратора убраны секция «График» и кнопка «Сохранить график», которая теперь упиралась бы в отказ. (3) Найденный владельцем пробел: колокольчик в шапке был у всех трёх кабинетов, а раздел «Уведомления» - только у владельца, притом что сервер уведомления администратору и мастеру отдавал и раньше (проверено на проде: GET /notifications отвечает 200 всем трём ролям). Раздел добавлен обоим - та же разметка и тот же источник данных, что у владельца; renderCenter из crm-notifications.js уже вызывался на всех страницах и молча не находил контейнер. Карточка «Уведомления на телефон» переехала из «Личных данных» в новый раздел, чтобы не двоиться. | **Прод: да.** Бэкенд `93e8bca`, фронт `60b8ea0`. Живая проверка на боевой системе: администратор и мастер получают 401 на попытку изменить смену (раньше администратор мог), в их кабинетах появился пункт меню «Уведомления» с рабочей лентой - вход выполнен в реальном браузере под обеими ролями. 833 офлайн-теста зелёные, живой прогон интерфейса 21 из 21. |
| 75 | 28.08 | api/routes/services.js · assets/crm-services-catalog.js · assets/crm-icons.js · crm-owner.html | **Каталог услуг арендатора в кабинете владельца.** До этого услуги заводились ровно один раз миграцией при первой сборке системы: второй арендатор (клиника Карины) остался без единой процедуры и без способа её добавить. Появились роуты создания, правки и удаления каталога (удаление честное, с отказом 409 и числом записей, если услуга уже стоит в истории) и раздел в кабинете - те же плитки `.service-check`, что и «Услуги и время» в карточке сотрудника, со своей иконкой вместо ножниц «Команды». | **Прод: да.** Живая проверка 29.08: `POST /services` и `DELETE /services/:id` отдают 401 (роут есть, закрыт авторизацией), `PUT /services/:id` тоже живой, `assets/crm-services-catalog.js` отдаётся GitHub Pages. |
| 76 | 29.08 | app.js · storage.js · tests/storage.public-catalog.test.js | **Каталог из кабинета доходит до публичного сайта.** Проверка по просьбе владельца вскрыла, что Окно 75 закрыло только половину пути: кабинет читает живой `/services`, а сайт брал услуги из списка, зашитого в `storage.js`. Живой прогон на прод-версии файлов: услуга, заведённая в кабинете и назначенная мастеру, на сайте не появлялась ни в форме записи, ни в прайсе, а удалённая из каталога продолжала показываться клиенту и приводила к отказу сервера на последнем шаге (`unknown_master_service`). Каталог сайта теперь собирается из `/public/masters` (он отдаёт услуги с именем, ценой и длительностью без логина): union по мастерам, цена и длительность - минимальные, порядок - `services.sort_order` с сервера. Описание состава подтягивается из статики по совпадению id, у новой услуги его нет - карточка прайса идёт без него. Зашитый список остался фоллбэком на случай, когда API не ответил или отдал пустоту: прайс сайта не должен схлопываться в ничто. | **Прод: нет** - правка лежит локально, ждёт команды на деплой. Проверено: 843 офлайн-теста, живые прогоны в headless-браузере (новая услуга видна в форме и прайсе, удалённая исчезает, переименование и цена доезжают, при мёртвом API страница остаётся на статике). |
| 77 | 30.08 | assets/crm-theme-clinic.css · api/lib/vertical-terms.js · assets/crm-terms.js · assets/crm-auth.js · tools/deploy-cabinet.mjs · tools/verify-2026-08-30-tema-kliniki.mjs | **Кабинет клиники говорит языком её сайта.** Слова арендатора приходили с сервера с Этапа B, а цвет оставался барбершопным: врач попадал с молочно-шампанского лендинга в зелёно-графитовый кабинет. Оформление стало таким же свойством вертикали, как склонения: `/tenant/appearance` отдаёт имя темы (`barbershop` → `default`, `clinic` → `clinic`), фронт ставит его атрибутом на `<html>`, красит CSS. Тема - дельта поверх «Дневного света», палитра один в один из `:root` сайта клиники (молоко #F7F3ED/#EFE8DE, графит #1E1C1A, шампань #B2915F, Cormorant Garamond + Inter). **Замер вместо глаза:** сайтовая шампань на молоке даёт 2.4:1 при пороге 4.5, поэтому в текст ушла углублённая ступень #7C5E33 (4.93:1 на полотне, 5.75:1 на карточке), а настоящая сайтовая шампань живёт в тёмном хроме (5.75:1 на графите). **Изоляция барбершопа** держится не обещанием: каждое правило файла висит на `[data-theme="clinic"]`, это проверяется разбором всех 86 селекторов, а сервер отдаёт барбершопу `default` - прогон падает, если хоть одно правило окажется без атрибута. **Находки прогонов:** (1) фото интерьера барбершопа (305 КБ) лежало в контуре клиники и стояло фоном её экрана входа - файл убран из выкладки, сверка выкладки научена не требовать намеренно отсутствующее; (2) герб Алихана в фоне рабочей области клиники бил в 404 - приём снят; (3) кэш темы затирался при недоступном сервере, то есть моргнувшая сеть перекрасила бы кабинет в чужое - теперь тема применяется только по успешному ответу, а кэш при провале не трогается; (4) название заведения на входе было набрано Playfair барбершопа - переведено на Cormorant. | **Прод: да** (30.08). Бэкенд Amvera `c7bb75b`, кабинет клиники `46aa177`, фронт GitHub Pages `c6dbb50`. Живая проверка после выкладки: боевой `/tenant/appearance` отдаёт клинике `theme: clinic`, барбершопу `theme: default`; в контуре клиники `crm-theme-clinic.css` отдаётся 200, фото интерьера барбершопа - **404, файла больше нет**; боевой кабинет клиники в браузере: тема `clinic`, полотно `rgb(239,232,222)`, шрифт Inter, акцент `#7C5E33`, картинка знака скрыта и показано название «Урбашевичус»; боевой кабинет Алихана в том же прогоне: тема `default`, Manrope, акцент `#8B6933`, локап и фото интерьера на месте. До выкладки: 844 теста проекта + 29 проверок темы; попиксельное сравнение кабинета барбершопа до и после правок - **0 отличающихся пикселей из 1 296 000**. |

**Пропуск 48/51/52** - не ошибка, зарезервированы под будущий контент Owner (Клиенты/Настройки/
адаптивность, см. `assets/crm-app-shell.js` STUB_NOTE и `plans/2026-08-09-admin-master-app-shell.md`).

**Индекс не обновлялся с Окна 46 по Окна 42-45 и по правкам 08-09.08 (роль мастера/скидки,
sidebar-полировка)** - они реально сделаны и в git, но без строк здесь. Не восстановлено в
рамках этой правки (вне её объёма) - см. память `project_barbershop-admin-master-app-shell-plan-09-08.md`.

**Сверка колонки "Прод" 11.08.2026 (Окно 55).** Строки 47/49/50 говорили "не задеплоено, коммит
локальный" - это было неверно: все три sha давно в `origin/main` (`git branch -r --contains`).
Урок тот же, что и по всему проекту: колонка "Прод" обновляется руками и потому стареет молча -
источник истины про деплой это `git branch -r --contains <sha>` и живой curl, не эта таблица.
Строка 46 (`GH - не задеплоено`) не сверялась в этот раз - там нет sha, проверять нечем.

**Не вошли отдельной строкой:** `ПРОМПТ-ОКНО-ТЕСТИРОВЩИК.md` и `ПРОМПТ-ОКНО-ДИЗАЙНЕР-AWWWARDS.md` -
не пронумерованные окна, роль-промпты (тестировщик/дизайнер), а не единицы функциональности.


**23.08.2026, побочная находка деплоя:** в корне репозитория лежал СИМЛИНК `node_modules -> api/node_modules`, не покрытый правилом `.gitignore` со слешем (`node_modules/`). Он уехал в коммит окна с `git add -A`, и сборка GitHub Pages упала на упаковке артефакта. Починено коммитом `719579e`: из индекса убран, в `.gitignore` добавлено правило без слеша, туда же локальные `.claude/settings.json` и `.claude/hooks/`.

## Как поддерживать актуальность

При заведении окна N+1 - добавить строку сюда (дата, файл, суть) сразу после коммита. При
деплое - обновить колонку "Прод" в тот же момент, не позже. Раз в 5-10 окон - быстрая живая
сверка (пара curl по характерным роутам), как сделано в этом аудите, чтобы ловить расхождения
раньше, чем через сотни строк `today.md`.
