-- Окно 10 (30.07.2026, разд.17.1 ТЗ): 002_schema.sql уже в BASELINE (см.
-- server.mjs runMigrations) и на живой базе повторно не выполнится, поэтому
-- правка текста в нём саму по себе живую строку не чинит - нужна отдельная
-- миграция, чтобы staff.name реально сменился на боевой базе (иначе, например,
-- при логине под master3-test@alikhan.test шапка CRM всё ещё покажет старое имя).
UPDATE staff SET name = 'Елизавета (пример)' WHERE id = 'master-3';
