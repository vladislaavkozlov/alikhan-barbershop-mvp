-- Мультиарендность, Этап B, Фаза 1 (24.08.2026,
-- plans/2026-08-24-multitenancy-etap-b-slovar.md).
-- Флаги модулей на уровне арендатора: раздел включается значением в справочнике,
-- а не условием по вертикали, размноженным по коду.
--
-- Только схема. Пустой объект означает «как по умолчанию для моей вертикали»
-- (api/lib/vertical-modules.js), поэтому у Алихана после этой миграции не меняется
-- ничего: он и так получает включённое всё. Свои значения арендатор получает строкой
-- в справочнике при подключении, а не здесь.
--
-- Замок справочника не трогаем: tenants - единственная таблица без RLS (миграция
-- 058), её читают ДО того, как арендатор известен. Клиентских данных в ней нет.

ALTER TABLE tenants
  ADD COLUMN IF NOT EXISTS modules jsonb NOT NULL DEFAULT '{}'::jsonb;
