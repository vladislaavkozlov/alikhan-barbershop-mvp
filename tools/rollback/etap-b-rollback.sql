-- Откат Этапа B (24.08.2026, plans/2026-08-24-multitenancy-etap-b-slovar.md).
--
-- Схему меняет одна миграция - 060_tenant_modules.sql, и меняет одним движением:
-- добавляет колонку флагов в справочник арендаторов. Данных она не трогает, поэтому
-- откат тоже один и не теряет ничего, кроме самих флагов.
--
-- ПОРЯДОК ВАЖЕН: сначала вернуть КОД (прежний бэкенд не знает про колонку и не просит
-- её в SELECT), потом выполнить этот файл. В обратном порядке живой код упадёт на
-- «column modules does not exist» между двумя шагами.
--
-- Словарь вертикали отката не требует вовсе: он живёт в коде, а не в базе.

ALTER TABLE tenants DROP COLUMN IF EXISTS modules;

DELETE FROM schema_migrations WHERE filename = '060_tenant_modules.sql';
